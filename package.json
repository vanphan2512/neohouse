{
  "name": "neohouse",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm-run-all",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-css": "node-sass scss/ -o css/",
    "watch-css": "npm run build-css && node-sass scss/ -o css/ --watch",
    "css-minify": "css-minify --dir css"
  },

  "author": "PhanNV",
  "license": "ISC",
  "dependencies": {
  },
  "devDependencies": {
    "node-sass": "^4.12.0"
  }

}
